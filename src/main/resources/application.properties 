# THIS FILE IS A DUPLICATE WITH A TRAILING SPACE IN THE FILENAME
# Please delete or rename this file. The canonical settings live in
# src/main/resources/application.properties (no trailing space).
# Kept to avoid accidental deletion by automation.

# Server configuration
server.port=8080
# Springdoc OpenAPI configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs

# Expose health and info endpoints
management.endpoints.web.exposure.include=health,info
# Show health details (set to 'when_authorized' or 'never' in production)
management.endpoint.health.show-details=always
