# Server port changed to avoid conflict with local services
server.port=8081

# Expose health and info endpoints
management.endpoints.web.exposure.include=health,info
# Show health details (for demo; use when_authorized in prod)
management.endpoint.health.show-details=always

# Use update to avoid schema drop/create issues with reserved names
spring.jpa.hibernate.ddl-auto=update

